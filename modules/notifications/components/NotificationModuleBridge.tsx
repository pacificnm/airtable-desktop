import { NotificationBusBridge } from './NotificationBusBridge.tsx'

/** Mount when the notifications module is enabled (see `App.tsx`). */
export function NotificationModuleBridge() {
  return <NotificationBusBridge />
}
