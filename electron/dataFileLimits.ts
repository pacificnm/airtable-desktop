/** Default page size for `files:listDataFileSpaces` when limit is omitted. */
export const DEFAULT_SPACE_DATA_FILE_PAGE_SIZE = 100

/** Hard cap per IPC response to keep the renderer responsive. */
export const MAX_SPACE_DATA_FILE_PAGE_SIZE = 500
