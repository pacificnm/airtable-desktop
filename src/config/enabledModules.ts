/**
 * Module ids to load at startup (modules/<id>/ or module-repos/<id>/).
 * Updated via Developer → Modules or edit manually.
 */
export const enabledModuleIds = ['config', 'notifications', 'location', 'space', 'region', 'buildingClassification', 'buildingStatus', 'city', 'country', 'state'] as const satisfies readonly string[]

